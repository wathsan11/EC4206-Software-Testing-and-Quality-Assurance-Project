on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get a clone of the repo
        uses: actions/checkout@v5

      - name: list all the files
        run: ls 

      - namw: run the tests
        run: ./mvnw test

        